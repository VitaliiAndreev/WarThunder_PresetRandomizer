<UserControl x:Class="Client.Wpf.Controls.ResearchTreeCellVehicleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Wpf.Controls"
             mc:Ignorable="d">

    <Border
        Style="{StaticResource ResearchTreeCell}">
        
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" HorizontalAlignment="Right">
                
                <TextBlock Text="Vehicle Name"
                    x:Name="_name"
                    Style="{StaticResource VehicleCard}"/>

            </Grid>
            
            <Grid Grid.Row="1" HorizontalAlignment="Right">
            </Grid>
            
            <Grid Grid.Row="2" HorizontalAlignment="Right">

                <TextBlock Text="00.0"
                    x:Name="_battleRating"
                    Style="{StaticResource VehicleCard}"/>
                
            </Grid>
        </Grid>
        
    </Border>

</UserControl>